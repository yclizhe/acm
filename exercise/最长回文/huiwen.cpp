#include <iostream>
#include <cstring>
using namespace std;
int dp[1000];
int main() {
	char str[1000];
	while(cin >> str) {
		memset(dp,1,sizeof(dp));
		int len = strlen(str);
		for(int i=1; i<len; i++) {
			if(dp[i-1] = i)
				dp[i] = 0;
			eles {
				
			}
		}



	}
}
